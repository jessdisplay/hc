/* One line to switch the intake form on.
   Paste the deployed Apps Script /exec URL between the quotes and push.
   Empty means the form tells people to call instead of failing silently. */
window.HC_HOOK = "";
